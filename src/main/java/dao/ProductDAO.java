package dao;

import model.Product;
/**
 * The ProductDAO class is a Data Access Object (DAO) for performing CRUD operations on Product entities.
 * It extends the AbstractDAO class, providing the necessary implementations for database operations.
 *
 * @param <Product> the type of entity this DAO manages, in this case, Product
 */
public class ProductDAO extends AbstractDAO<Product> {
/**
 * Constructs a new instance of the ProductDAO class.
 * It inherits the CRUD operations and database access functionality from the AbstractDAO class.
 */
}
