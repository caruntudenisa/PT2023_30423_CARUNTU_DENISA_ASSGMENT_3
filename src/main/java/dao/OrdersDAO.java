package dao;

import model.Orders;
/**
 * The OrdersDAO class is a Data Access Object (DAO) for performing CRUD operations on Orders entities.
 * It extends the AbstractDAO class, providing the necessary implementations for database operations.
 *
 * @param <Orders> the type of entity this DAO manages, in this case, Orders
 */

public class OrdersDAO extends AbstractDAO<Orders> {
    /**
     * Constructs a new instance of the OrdersDAO class.
     * It inherits the CRUD operations and database access functionality from the AbstractDAO class.
     */
}
