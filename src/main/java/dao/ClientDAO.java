package dao;

import model.Client;
/**
 * The ClientDAO class is a Data Access Object (DAO) for performing CRUD operations on Client entities.
 * It extends the AbstractDAO class, providing the necessary implementations for database operations.
 *
 * @param <Client> the type of entity this DAO manages, in this case, Client
 */
public class ClientDAO extends AbstractDAO<Client> {
    /**
     * Constructs a new instance of the ClientDAO class.
     * It inherits the CRUD operations and database access functionality from the AbstractDAO class.
     */
}
